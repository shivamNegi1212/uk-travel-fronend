/**
 * PassengerDashboard: Main passenger interface for ride booking
 * This is an alias to PassengerHome with all the same features:
 * - Browse available rides
 * - Filter by location & date
 * - View driver ratings
 * - Book rides with seat selection
 * - Track booking status
 */
import PassengerHome from './PassengerHome'

export default PassengerHome
